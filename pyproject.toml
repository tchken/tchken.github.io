[tool.poetry]
name = "tchken-portfolio-scripts"
version = "0.1.0"
description = "Image generation scripts for portfolio"
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
Pillow = "^10.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
